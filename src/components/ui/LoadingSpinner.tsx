import React from 'react';
import { View, Text } from 'react-native';

const LoadingSpinner = () => {
  return (
    <View>
      <Text>Loading Spinner Component</Text>
    </View>
  );
};

export default LoadingSpinner;
